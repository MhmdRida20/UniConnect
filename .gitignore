# Build results
[Bb]in/
[Oo]bj/
[Oo]ut/
[Ll]og/
[Ll]ogs/

# Visual Studio temporary files
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# Build artifacts
*.dll
*.exe
*.pdb
project.assets.json
project.nuget.cache
*.nuget.dgspec.json
*.nuget.g.props
*.nuget.g.targets

# Rider / VS Code
.idea/
.vscode/

# OS files
Thumbs.db
.DS_Store

# App secrets (optional — keep local connection strings private)
appsettings.Development.json

# Archives. A build/source zip was once committed here by accident and put
# 88 MB into the history permanently — git keeps every version of a binary
# forever, so the repo can only ever grow. Hand these over out of band.
*.zip
*.7z
*.rar